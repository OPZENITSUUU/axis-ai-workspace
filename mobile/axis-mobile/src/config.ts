/**
 * The native companion deliberately uses the published AXIS origin. Manus OAuth,
 * user-scoped API access, and session cookies remain on that trusted HTTPS domain;
 * provider keys never enter the Android bundle.
 */
export const AXIS_WEB_URL = "https://persaiassist-u5z3cgkj.manus.space";
